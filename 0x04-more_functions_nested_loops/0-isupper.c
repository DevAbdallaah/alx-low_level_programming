#include "main.h"
#include "ctype.h"

/**
 * _isupper - function determines upper case letters
 * @c: is the parameter
 * Return: 1 if upper case
 *         0 otherwise
 */

int _isupper(int c)
{
if (isupper(c))
return (1);
else
return (0);
}
